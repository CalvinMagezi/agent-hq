# Convex Backend
NEXT_PUBLIC_CONVEX_URL=

# API Key for Convex HTTP endpoints
# Default: "local-master-key" (for local development only)
AGENTHQ_API_KEY=

# LLM Configuration
OPENROUTER_API_KEY=
GEMINI_API_KEY=
DEFAULT_MODEL=gemini-2.5-flash

# Provider routing:
# - "gemini-*" or "google/gemini-*" models use GEMINI_API_KEY directly (if set)
# - All other models use OPENROUTER_API_KEY
# - Gemini models fall back to OpenRouter if GEMINI_API_KEY is not set

# Agent Working Directory (optional)
# TARGET_DIR=

# Security: Worker Secret (64-character hex string)
# Generate with: openssl rand -hex 32
# IMPORTANT: Must match WORKER_SECRET in Convex dashboard
WORKER_SECRET=
