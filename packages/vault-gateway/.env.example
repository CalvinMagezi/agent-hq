# vault-gateway environment configuration
# Copy this file to .env and fill in your values.
# .env is gitignored and must NEVER be committed.

# ─── Obsidian Local REST API ─────────────────────────────────────────────────
# API key from the Obsidian Local REST API plugin settings
OBSIDIAN_API_KEY=your-obsidian-api-key-here

# Host and port of the Obsidian REST plugin (defaults shown)
OBSIDIAN_REST_HOST=127.0.0.1
OBSIDIAN_REST_PORT=27124

# ─── Gateway ─────────────────────────────────────────────────────────────────
# Port the gateway listens on (default: 3001)
GATEWAY_PORT=3001

# ─── Agent Tokens (auth) ─────────────────────────────────────────────────────
# Comma-separated list of agent credentials in the format:
#   token:role:id
# Roles: admin | relay | readonly
#
# Generate secure random tokens (example):
#   openssl rand -hex 32
#
# Example:
AGENT_TOKENS=replace-with-strong-random-token:admin:hq-admin,replace-with-another-token:relay:discord-relay

# ─── Testing only ────────────────────────────────────────────────────────────
# Set to "true" to bypass the Obsidian proxy and return mock responses
# MOCK_OBSIDIAN=true
