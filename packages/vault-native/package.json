{
  "name": "@repo/vault-native",
  "version": "0.0.0",
  "main": "index.ts",
  "types": "index.ts",
  "napi": {
    "binaryName": "vault-native",
    "package": {
      "name": "@repo/vault-native-native"
    }
  },
  "scripts": {
    "artifacts": "napi artifacts",
    "build": "napi build --platform --release --js-package-name @repo/vault-native-native",
    "build:debug": "napi build --platform --js-package-name @repo/vault-native-native",

    "prepublishOnly": "napi prepublish -t npm",
    "test": "bun test",
    "version": "napi version"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.5.1"
  }
}
